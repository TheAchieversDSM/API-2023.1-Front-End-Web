import axios, { AxiosInstance } from "axios";

const url = {
    baseURL: "http://52.15.39.3:5000",
    timeout: 1000,
    headers: {
        "Content-Type": "application/json",
    }
};

export default url;